# Common configuration for all Hamiltonian Cycle scripts

# Grid Size (Must be even for Hamiltonian Cycle on grid)
N = 64

# Visualization Settings
SVG_SIZE = 800  # Fixed size for the SVG canvas (pixels)
MARGIN = 20     # Margin around the grid
